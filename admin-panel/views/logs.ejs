<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>VPN Admin Panel - Logs</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <style>
        body { background-color: #f8f9fa; }
        .sidebar {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
            color: white;
        }
        .nav-link {
            color: rgba(255, 255, 255, 0.8);
            border-radius: 8px;
            margin: 2px 0;
        }
        .nav-link:hover, .nav-link.active {
            background: rgba(255, 255, 255, 0.1);
            color: white;
        }
        .card {
            border: none;
            border-radius: 15px;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
        }
        .content-wrapper {
            margin-left: 250px;
            padding: 20px;
        }
        .log-entry {
            font-family: 'Courier New', monospace;
            font-size: 0.9em;
        }
        .log-info { color: #17a2b8; }
        .log-warning { color: #ffc107; }
        .log-error { color: #dc3545; }
        @media (max-width: 768px) {
            .content-wrapper {
                margin-left: 0;
            }
        }
    </style>
</head>
<body>
    <div class="d-flex">
        <!-- Sidebar -->
        <nav class="sidebar position-fixed top-0 start-0" style="width: 250px; z-index: 1000;">
            <div class="p-3">
                <h4><i class="fas fa-shield-alt me-2"></i>VPN Admin</h4>
                <hr class="text-white">
                
                <ul class="nav flex-column">
                    <li class="nav-item">
                        <a class="nav-link" href="/dashboard">
                            <i class="fas fa-tachometer-alt me-2"></i>Dashboard
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/users">
                            <i class="fas fa-users me-2"></i>VPN Users
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/connections">
                            <i class="fas fa-link me-2"></i>Connections
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link active" href="/logs">
                            <i class="fas fa-file-alt me-2"></i>System Logs
                        </a>
                    </li>
                    <% if (user.role === 'admin') { %>
                    <li class="nav-item">
                        <a class="nav-link" href="/settings">
                            <i class="fas fa-cog me-2"></i>Settings
                        </a>
                    </li>
                    <% } %>
                </ul>
                
                <hr class="text-white">
                
                <div class="dropdown">
                    <a href="#" class="nav-link dropdown-toggle" data-bs-toggle="dropdown">
                        <i class="fas fa-user me-2"></i><%= user.username %>
                    </a>
                    <ul class="dropdown-menu">
                        <li><a class="dropdown-item" href="/logout">
                            <i class="fas fa-sign-out-alt me-2"></i>Logout
                        </a></li>
                    </ul>
                </div>
            </div>
        </nav>
        
        <!-- Main Content -->
        <div class="content-wrapper">
            <div class="container-fluid">
                <!-- Header -->
                <div class="row mb-4">
                    <div class="col">
                        <h2>System Logs</h2>
                        <p class="text-muted">Monitor system events and activities</p>
                    </div>
                </div>
                
                <!-- Logs -->
                <div class="card">
                    <div class="card-body">
                        <div class="log-container" style="max-height: 600px; overflow-y: auto;">
                            <% logs.forEach(log => { %>
                            <div class="log-entry mb-2 p-2 border-bottom">
                                <span class="text-muted"><%= new Date(log.timestamp).toLocaleString() %></span>
                                <span class="badge bg-<%= log.level === 'error' ? 'danger' : log.level === 'warning' ? 'warning' : 'info' %> ms-2">
                                    <%= log.level.toUpperCase() %>
                                </span>
                                <div class="mt-1 log-<%= log.level %>"><%= log.message %></div>
                            </div>
                            <% }); %>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
    
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
